server:
  port: 8080

logging:
  level:
    root: info
    com.qboot: debug

spring:
  application:
    name: app-auth
#  flyway:
#    enabled: false
#    locations: classpath:db/migration
#    validate-migration-naming: true
